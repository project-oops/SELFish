[package]
name = "selfish-bytes"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Bounds-checked fixed-width integer reads and writes"

[dependencies]

[lints]
workspace = true
